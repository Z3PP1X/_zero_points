# grid.yaml — STUFE 6 (Hetero + DiffPool)
# 2 x 2 x 2 x 1 = 8 Konfigurationen.
# layer_type/att_heads sind im Hetero-Pfad inert (GINConv via to_hetero) -> nicht gesweept.

gnn.dim_inner:
  - 64
  - 128

gnn.layers_mp:
  - 2
  - 4

gnn.variant:
  - legacy
  - pooling

gnn.pool_type:
  - diffpool

gnn.dropout:
  - 0.0
