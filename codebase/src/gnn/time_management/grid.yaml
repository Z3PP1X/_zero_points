# grid.yaml
# Grid definitions for sweeping over architectures during inference timing

gnn.layer_type:
  - sageconv
  - gcnconv
  - ginconv
  - gatv2conv

gnn.layers_mp:
  - 3

gnn.dim_inner:
  - 64
  - 128
  - 256
  - 512

model.graph_pooling:
  - add
